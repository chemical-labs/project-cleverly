import React, { Component } from 'react'
import { View, Text, TouchableOpacity, AsyncStorage, Image, ScrollView } from 'react-native'
import axios from 'axios'
import konfigurasi from '../../config'

export default class Upload_Materi extends Component{
    render(){
        return(
            <View>
            </View>
        )
    }
}
